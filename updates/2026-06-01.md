# 2026-06-01 — Spec #1: Model lineup + tier-aware context + token metering

Brainstormed and applied a five-section spec covering: simplified model lineup,
tier-aware history budgets, plan-aware CLI, token-based metering, and a signup
nudge for anon users. Also applied four edge-case fixes surfaced during review.

## What was applied

### Section 1 — Model lineup
- **Server** [`server/src/routes/proxy.ts`]: replaced `MiniMax/DeepSeek/Kimi` table with a simpler `Qwen3-Coder / DeepSeek` table.
  - `anon` + `free` → `Qwen/Qwen3-Coder-480B-A35B-Instruct-FP8` ($2/$2 per 1M tokens)
  - `pro` → `deepseek-ai/DeepSeek-V4-Pro` ($2.10/$4.40 per 1M tokens, cached input $0.20/M)
  - Classifier (internal): `Qwen2.5-7B-Instruct-Turbo` (unchanged)
- Removed: MiniMax M2.7, Kimi K2.6 (previously reserved for multimodal — dropped from active table)
- Legacy CLI hints (old vendor names) fall back to the plan's quick model — no breaking changes.

### Section 2 — Tier-aware history budgets
- **New file** [`CLI/src/llm/budgets.ts`]: `HISTORY_BUDGET` and `COMPACT_AT` keyed by plan.

| Tier | History budget | Compact at | Equivalent turns |
|---|---|---|---|
| anon | 40k chars | 30k chars | ~10 |
| free | 80k chars | 60k chars | ~20 |
| pro  | 200k chars | 150k chars | ~50 |

- `trimHistory(messages, plan)` and `compactOldMessages(messages, llm, plan)` are now tier-aware.
- Old global `MAX_HISTORY_CHARS = 60_000` removed from agent.ts.

### Section 3 — Plan-aware CLI
- **Server** [`server/src/routes/proxy.ts`]: every response now sets `X-Glitool-Plan`, `X-Glitool-Resolved-Model`, `X-Glitool-Tokens-Used`, `X-Glitool-Tokens-Limit` headers.
- **CLI** [`CLI/src/auth.ts`]: added `Plan` type, `persistPlan()`, `getCurrentPlan()`.
- **CLI** [`CLI/src/llm/factory.ts`]: `makeCaptureFetch` captures all four headers per request. New export `getTokenUsageForCurrentRequest()`.
- BYOK (`OPENAI_API_KEY` set) is treated as `pro` for budget decisions.
- Cold-start lag: at most 1 call before cached plan corrects via server header.

### Section 4 — Token metering & enforcement
- **Server** [`server/src/models/index.ts`]: added `tokens_used` field to `AnonUsage` and `Usage` (additive, default 0).
- **Server** [`server/src/middleware/checkUsageLimit.ts`]: completely rewritten — checks `tokens_used` against `TOKEN_LIMITS`.

| Tier | Cap | Server cost |
|---|---|---|
| anon | 50k tokens lifetime | ~$0.10 per user |
| free | 500k tokens / month | ~$1.00 / user / month |
| pro  | 3M tokens / month hard cap | ~$9-13 / user / month |

- 429 responses include `tier`, `tokens_used`, `tokens_limit`, helpful message, and `signup_url` / `upgrade_url` where relevant.
- **Server** [`server/src/routes/proxy.ts`]:
  - Streaming branch now parses SSE bytes to extract the final `usage` chunk (Together.ai's `stream_options.include_usage: true` was already set, we just weren't reading it).
  - Non-streaming branch already had `data.usage` — now uses it.
  - `trackUsage(req, tokens)` `$inc`s both `tokens_used` (new) and `request_count` (kept for telemetry).
- Enforcement is soft: a user at 49.5k can fire one more call and end at ~60k. Industry-standard fail-open.
- Request-ID dedup still applies: one user prompt = one token-counting event, even with 10+ ReAct iterations.

### Section 5 — Auto-compaction signup nudge
- **CLI** [`CLI/src/agent.ts`]: module-scope `nudgeShownThisSession` flag. New `onNudge` callback in `chat()`. Fires once per process when `plan === 'anon'` and compaction just happened. `clearSession()` resets the flag.
- **CLI** [`CLI/src/ui/App.tsx`]: added `'nudge'` to `Message.role`. Wired callback. Renders as `<Text color="yellow" dimColor>`.
- Nudge text: `✨ Conversation getting long — /signup gives you 2× more memory and 10× more tokens.`

### Four edge-case fixes (applied after section review)

1. **trimHistory keep-one guard** [`CLI/src/agent.ts`]: always keeps at least the most recent message even if it alone exceeds the tier budget. Prevents empty history when a single user paste is bigger than the budget.

2. **NaN token guard** [`server/src/routes/proxy.ts`]: `trackUsage` now checks `Number.isFinite(tokens)` before `$inc`. If Together returns malformed `usage`, we skip the write rather than corrupt the DB.

3. **Usage-missing warning** [`server/src/routes/proxy.ts`]: `console.warn` when the 200 response has no `usage` field (streaming or non-streaming). Surfaces silent token-capture failures in logs so we notice fast.

4. **Lower compaction min-messages threshold** [`CLI/src/memory.ts`]: was `messages.length <= KEEP_RECENT + 4`, now `<= KEEP_RECENT`. Lets compaction fire on smaller conversations where one early message is huge.

## Edge cases NOT covered (remaining)

These are edge cases identified during spec review that we deliberately did not implement, with reasoning:

### Server / model layer
- **Together.ai 503 / rate limit** — error is passed through to CLI. No retry, no fallback chain. Decision: handle if logs show this happening; not a hypothetical worth pre-building.
- **Qwen3-Coder de-serverless'd** — would require a one-line constant change + redeploy. Acceptable hot-patch path.

### Plan detection
- **`/logout` command** — not yet implemented. User has to manually delete `~/.glitool/auth.json` to log out. Pre-existing gap, out of scope here.
- **Stale `pro` plan after token revocation** — cached `auth.json` could say `pro` after admin revokes the token. CLI gets 401 on next call, but never refreshes the cached plan. No real harm — they can't make calls anyway.
- **Two CLI processes writing `auth.json` simultaneously** — last-write-wins, both writing the same plan in normal usage. Zero impact except in the very narrow downgrade-while-running scenario.

### Token metering
- **Stream interrupted mid-response (Ctrl+C)** — best-effort charging based on what arrived before disconnect. Usually correct since Together emits usage near the end.
- **Mongo write fails during trackUsage** — one free call per Mongo blip. Logged but not retried. Acceptable rarity.
- **Server restart mid-request** — dedup map in memory; restart clears it. A retry of the same request_id after restart could double-charge. Window is seconds.
- **CLI doesn't render 429 token_limit_exceeded prettily** — we send the friendly message + URL in the JSON, but the CLI error path may show it as a raw `Error: 429` until we add a specific handler. **Needs smoke test.**

### Compaction / nudge
- **Single oversized paste in fresh session** — Fix 1 above handles the immediate UX (overshoot the budget, send anyway). Fix 4 also lowers the compaction threshold. Working as intended now.

## What's left

### Spec #1 — final section
- **Section 6 — Migration & rollout**
  - Deploy order: **server first** (so the new headers are live before any new CLI sends a `glitool/coder` hint), CLI second.
  - Database: no migration script needed — `tokens_used` defaults to 0 on read.
  - Existing users get an effective clean reset on token budgets. Acceptable.
  - Smoke tests to add:
    1. Anon user hits the new 50k cap, sees 429 + signup hint
    2. Free user runs heavy coding, observes Qwen3-Coder via `X-Glitool-Resolved-Model`
    3. Pro user observes DeepSeek V4 Pro routing
    4. Anon compaction triggers nudge once, not twice
    5. `/clear` restores nudge eligibility
  - Version bump: CLI 2.1.0 → 2.2.0 (minor — new behavior, no breaking API)
  - Publish to npm + push tags

### Other deferred items (carried over from earlier sessions)
- **StatusBar bug** — still reads `0 tokens · $0.00` after executor runs. Should now wire `getTokenUsageForCurrentRequest()` from factory.ts.
- **`"No output."` literal** — [`CLI/src/agents/executor.ts:171`](CLI/src/agents/executor.ts#L171) still emits this when the ReAct loop ends without final text.
- **Windows shell dialect** — executor BASE_PROMPT still doesn't tell the model to use `Get-ChildItem` instead of `ls` on Windows.

### Spec #2 — monitoring dashboard
Brainstorming not yet started. The data foundation is now in place (token counts per user, plan field, model resolution headers) so a dashboard has real data to display. Open questions for that spec:
- Audience: admin-only on a separate URL, or self-serve `/usage` for end users?
- Tech stack: extend the Next.js client at glit.in, or a standalone admin app?
- Auth: GitHub OAuth (you sign in same way), or a separate admin password?

Will start that brainstorm when ready.

## Files touched in this session

**Server** (3 files):
- `server/src/models/index.ts`
- `server/src/middleware/checkUsageLimit.ts`
- `server/src/routes/proxy.ts`

**CLI** (6 files):
- `CLI/src/llm/budgets.ts` (NEW)
- `CLI/src/auth.ts`
- `CLI/src/llm/factory.ts`
- `CLI/src/memory.ts`
- `CLI/src/agent.ts`
- `CLI/src/ui/App.tsx`

## Build status
- CLI: `tsc` clean
- Server: `tsc` clean
- Nothing published or deployed yet — that's Section 6.

---

# 2026-06-01 (session 2) — Local testing + six bug fixes

Tested Spec #1 against the local server, then fixed six bugs surfaced by the
monitoring dumps and live runs. Everything below is built clean and the server
change is live on the local `tsx watch`; **none of it is published/deployed yet**
(still pending Section 6).

## Local-server verification (Spec #1, Sections 1/3/4)
Ran curl smoke tests against `http://localhost:3000` (server `npm run start:local`):

| Check | Result |
|---|---|
| Anon → model routing (Sec 1) | `Qwen/Qwen3-Coder-480B-A35B-Instruct-FP8` ✓ |
| Plan-aware headers (Sec 3) | All four `X-Glitool-*` headers present ✓ |
| Token metering (Sec 4) | `tokens_used` increments per new request ✓ |
| Request-ID dedup | repeat same id did NOT double-count ✓ |
| 429 at cap (Sec 4) | seeded anon at 50k → 429 with `tier`/`tokens_*`/`signup_url` ✓ |

Not curl-testable: free/pro model routing distinction (needs pro token), Sec 2/5
(CLI-side). Test docs seeded in `anonusages` were cleaned up.

## Bug fixes

### 1. Recursion limit error on open-ended prompts
- **Symptom:** `Error: Recursion limit of 25 reached without hitting a stop condition` on prompts like "read the project and understand it".
- **Cause:** the chat-fallback agent ([`CLI/src/agent.ts`]) called `streamEvents` with no `recursionLimit` → LangGraph default 25. The model looped through file reads past 25 steps. (Executor already used 50.)
- **Fix:** set `recursionLimit: 50` on the chat agent, and wrapped the stream loop in a `try/catch` that catches `GraphRecursionError` and degrades to a friendly message ("I went in circles… narrow the request") instead of a raw stack trace.

### 2. StatusBar stuck at `0 tokens · $0.00` after executor runs
- **Cause:** the executor branch returned only a string and `emit('done', { total_tokens: 0 })`, never calling `onUsage` — so coding/agentic tasks never updated the StatusBar.
- **Fix:** `runExecutor` now accumulates `usage_metadata` per `on_chat_model_end` and returns `{ text, tokensIn, tokensOut }`; the executor branch in `agent.ts` fires `onUsage(tokens, estimateCost(...))` with the real resolved model and emits the true `total_tokens`. Mirrors the chat path.

### 3. Welcome banner showed stale `v1.0.1`
- **Cause:** [`CLI/src/ui/App.tsx`] hardcoded `version="1.0.1"`; not read from `package.json` (currently 2.1.0).
- **Fix:** threaded `pkg.version` through as an `App` prop (`AppProps.version`), passed from `index.tsx`. Single source of truth — tracks the package version automatically.
- **Noted, not fixed:** `Welcome back, Developer.` — the name is the hardcoded `DEFAULTS.name` in [`CLI/src/config.ts`], not the signed-in GitHub identity. Left as a follow-up.

### 4. Server massively UNDER-counted tokens (economic bug) 🔴
- **Cause:** `trackUsage` ([`server/src/routes/proxy.ts`]) bailed out on `wasAlreadyCounted(reqId)`, so only the **first** ReAct iteration's tokens hit `tokens_used`. A 16-iteration "read the project" charged ~3.4k but cost ~130k on Together (~38× undercount). The dedup was designed for *request* counting, not *token* counting.
- **Fix:** split the two counters — `request_count` stays deduped (1 prompt = 1 request), but `tokens_used` now `$inc`s on **every** iteration (each is real billable provider cost). Verified live: two same-request-id calls → DB `tokens_used: 45`, `request_count: 1`.
- **Consequence:** agentic prompts now weigh much heavier against quota (correct). Combined with fix #5, real usage should be saner. Watch after rollout.

### 5. Model looped on identical tool calls
- **Symptom:** monitor flagged `⚠ LOOP ×3` — `searchCode exportTasks` and `readFile taskService.ts` each called 3×.
- **Cause:** the chat-fallback agent prompt had no no-repeat guard (executor domains do).
- **Fix:** added a "Tool discipline (avoid loops)" block to the chat system prompt in [`CLI/src/agent.ts`]: never call the same tool with the same input twice, read each file once, finish exploration in a few calls.

### 6. Executor claimed work it never did (fabrication) 🔴
- **Symptom:** "fix the design of task-page.html" → agent reported a full redesign (green gradient, DOCTYPE, card layout). Reality: `task-page.html` untouched (confirmed via git + grep). The dump showed **zero TOOL CALL events** — the model emitted an entire fake ReAct trace as plain text in one completion.
- **Root cause:** history pollution. The chat path persisted `finalResponse` = every streamed token, including mid-turn narration ("Let me check… I've updated…"). Fed back as history with no tool records, the model learned to imitate narration-as-answer.
- **Three-layer fix:**
  - **Layer 1 (executor guard):** track tool-call count; if a run made 0 tool calls but the text claims work (`claimsFileAction` heuristic), retry once with a forced-action nudge; if it still won't act, return an honest "no changes were made" notice instead of the lie. Both passes' tokens accumulate. Emits `⚠ warning` events.
  - **Layer 2 (de-poison history):** chat path now persists only `lastModelText` (the final message after the last tool call), not the accumulated narration. Live display unchanged.
  - **Layer 3 (tool binding):** verified — no `tool_choice` override, `createReactAgent` binds with default `"auto"`, other sessions prove tools fire. No config bug; purely model behavior, handled by Layers 1/2.
  - Deliberately did **not** apply the aggressive guard to the chat path (it legitimately answers coding questions with no tools → false positives).

## Deeper follow-ups identified (not done)
- **Full tool-step history.** Layer 2 removes the narration poison, but properly recording AIMessage(tool_calls)+ToolMessage pairs in history would be the complete cure. Risky — `trimHistory`/compaction would need to stay tool-call/tool-result aware (OpenAI 400s on orphaned tool calls). Deferred.
- **Welcome name** from GitHub identity (see fix #3).
- The four carried-over items from session 1 (StatusBar now addressed; `"No output."`, Windows shell dialect still open).

## Files touched this session
**Server (1):** `server/src/routes/proxy.ts`
**CLI (4):** `CLI/src/agent.ts`, `CLI/src/agents/executor.ts`, `CLI/src/ui/App.tsx`, `CLI/src/index.tsx`

## Build status (session 2)
- CLI: `tsc` clean
- Server: `tsc` clean
- Server fix #4 live on local `tsx watch`; nothing published/deployed. Section 6 still pending.
